# 🚀 SEDAI Solar2Grid - 快速启动指南

## 📱 在线演示视频/截图

由于当前项目部署在本地环境，建议您录制以下演示内容：

### 推荐演示流程（5-10分钟）

1. **Overview 页面** (30秒)
   - 展示 KPI 看板
   - 模型健康灯（绿/黄/红状态）
   - 今日预测电量、成本、弃光、峰值购电指标

2. **Forecast 页面** (1分钟)
   - 点击刷新按钮触发预测
   - 展示 p10/p50/p90 概率带图表
   - 查看驱动因素说明卡片

3. **Dispatch 页面** (1分钟)  
   - 展示 24h 调度时间线
   - 点击展开查看 reason 解释
   - 切换图表/列表视图
   - 点击重算按钮

4. **Alerts 页面** (30秒)
   - 展示不同严重程度告警
   - 演示 ACK 确认功能

5. **Sites 页面** (30秒)
   - 站点切换
   - 模型验证卡片

---

## 🖥️ 本地快速启动（10分钟内运行）

### 前置要求
- Docker Desktop
- Flutter SDK (3.0+)
- Python 3.10+

### 步骤 1: 启动后端服务

```powershell
# 进入项目根目录
cd "c:\Users\TIAN FENG\Desktop\SDEA\sedai-solar2grid"

# 启动所有后端服务（数据库、API、预测、调度）
cd infra
docker-compose up -d

# 等待服务启动（约 30 秒）
# 验证服务状态
docker-compose ps

# 查看日志（可选）
docker-compose logs -f api forecast dispatch
```

**服务端口**:
- API Gateway: http://localhost:8000
- Forecast Service: http://localhost:8001
- Dispatch Service: http://localhost:8002
- Database: localhost:5432
- MinIO Console: http://localhost:9001

### 步骤 2: 配置 Flutter App

```powershell
# 进入 Flutter 项目目录
cd ..\apps\mobile_flutter

# 安装依赖
flutter pub get

# 检查设备（选择一个）
flutter devices

# 方案 A: 在 Chrome 浏览器中运行（推荐，用于演示）
flutter run -d chrome --web-port=3000

# 方案 B: 在 Android 模拟器中运行
flutter run -d emulator-5554

# 方案 C: 在 Windows 桌面运行
flutter run -d windows
```

### 步骤 3: 初始化数据（首次运行）

启动 Flutter App 后：

1. **选择站点**: 在 Sites 页面选择 `demo-site-001`
2. **生成预测**: 切换到 Forecast 页面，点击右上角刷新按钮
3. **生成调度**: 切换到 Dispatch 页面，点击右上角刷新按钮
4. **查看 KPIs**: 返回 Overview 页面查看所有指标

---

## 📸 截图建议

### 1. Overview 页面
```
┌─────────────────────────────────────┐
│ 📍 demo-site-001                    │
│ 100 kW Solar + 200 kWh BESS        │
├─────────────────────────────────────┤
│ 🚦 Model Health: [●] GREEN         │
│    Drift: 5.2% | NRMSE: 0.12       │
├─────────────────────────────────────┤
│ ☀️ Predicted Energy                 │
│    450.5 kWh  (Today's forecast)   │
├─────────────────────────────────────┤
│ 💰 Today's Cost                     │
│    $24.50  (Optimized dispatch)    │
├─────────────────────────────────────┤
│ ⚡ Curtailment                       │
│    12.3 kWh  (Solar curtailed)     │
├─────────────────────────────────────┤
│ 🌊 Peak Import                      │
│    35.2 kW  (Max grid import)      │
└─────────────────────────────────────┘
```

### 2. Forecast 页面
```
┌─────────────────────────────────────┐
│ Solar Forecast              [🔄]   │
├─────────────────────────────────────┤
│  Site: demo-site-001               │
│  450.5 kWh | Peak: 98.2 kW        │
├─────────────────────────────────────┤
│        📈 P10/P50/P90 Chart        │
│  100├────────────────────          │
│   kW│      ╱╲    ╱╲                │
│   50│    ╱    ╲╱    ╲              │
│    0└─────────────────────         │
│       6h   12h   18h               │
│                                     │
│  ━━━ P50 (Median)                  │
│  ░░░ P10-P90 Range                 │
├─────────────────────────────────────┤
│ 💡 Driving Factors                 │
│  ☀️ Solar Irradiance               │
│     Clear sky model baseline       │
│  ☁️ Cloud Cover                     │
│     NWP data adjusts baseline      │
│  🌡️ Temperature                     │
│     Module efficiency (-0.4%/°C)   │
│  ⚙️ System Parameters               │
│     PR, soiling calibrated         │
└─────────────────────────────────────┘
```

### 3. Dispatch 页面
```
┌─────────────────────────────────────┐
│ Energy Dispatch    [📊] [🔄]       │
├─────────────────────────────────────┤
│ ▼ 08:00  SOC: 65%                  │
│  ℹ️ Why this schedule?              │
│     Morning ramp-up: charge battery│
│     with cheap grid power before   │
│     peak tariff starts at 09:00    │
│                                     │
│  🟠 PV Output: 45.2 kW             │
│  🔵 Battery Charge: 30.0 kW        │
│  🟢 Battery Discharge: 0.0 kW      │
│  🔴 Grid Import: 12.5 kW           │
│  🟣 Grid Export: 0.0 kW            │
│  ⚪ Curtailment: 2.7 kW            │
│  🟢 SOC: 65.0%                     │
├─────────────────────────────────────┤
│ ▶ 08:15  SOC: 67%                  │
│ ▶ 08:30  SOC: 70%                  │
│ ...                                 │
└─────────────────────────────────────┘
```

### 4. Alerts 页面
```
┌─────────────────────────────────────┐
│ Alerts                       [🔄]  │
├─────────────────────────────────────┤
│ 🔴 [CRITICAL]            [ACK ✓]   │
│   2026-02-09 08:45                 │
│   Model drift exceeded 25%         │
│   Immediate calibration required   │
├─────────────────────────────────────┤
│ 🟠 [HIGH]                [ACK ✓]   │
│   2026-02-09 07:30                 │
│   Battery SOC dropped below 20%    │
├─────────────────────────────────────┤
│ 🟡 [MEDIUM]       [ACKNOWLEDGED]   │
│   2026-02-09 06:15                 │
│   Grid connection unstable         │
│   Acknowledged at: 06:20           │
└─────────────────────────────────────┘
```

---

## 🎬 录制视频建议

### 工具推荐
- **OBS Studio** (免费): 最强大，支持多场景切换
- **ShareX** (免费): 简单易用，支持 GIF 录制
- **Loom** (在线): 自动上传到云端，可分享链接

### 录制设置
```
分辨率: 1920x1080 或 1280x720
帧率: 30 fps
格式: MP4 (H.264)
音频: 可选（旁白讲解会更好）
```

### 视频结构（5分钟版本）
```
00:00-00:10  标题动画 + 项目介绍
00:10-00:40  Overview 页面演示
00:40-01:40  Forecast 页面演示（含刷新）
01:40-02:40  Dispatch 页面演示（含重算）
02:40-03:10  Alerts 页面演示
03:10-04:30  完整流程演示（站点切换→预测→调度→查看结果）
04:30-05:00  技术栈介绍 + 结尾
```

---

## 🌐 部署到公网（可选）

如果您想将演示链接分享给他人，可以：

### 方案 A: Vercel/Netlify（仅前端）
```bash
# Flutter Web 构建
cd apps/mobile_flutter
flutter build web

# 部署到 Vercel
npm i -g vercel
vercel --prod build/web
```

### 方案 B: Railway/Render（全栈）
1. 将代码推送到 GitHub
2. 在 Railway 创建项目
3. 连接 GitHub 仓库
4. 自动部署

### 方案 C: 云服务器（AWS/Azure/阿里云）
```bash
# 使用 docker-compose 一键部署
scp -r sedai-solar2grid user@server:/app
ssh user@server
cd /app/sedai-solar2grid/infra
docker-compose up -d
```

---

## 📊 数据示例

如果需要更真实的演示数据，可以运行：

```powershell
# 生成模拟数据（Python 脚本）
python scripts/generate_demo_data.py --site demo-site-001 --days 7

# 或使用 API 批量生成
curl -X POST http://localhost:8000/demo/populate \
  -H "Content-Type: application/json" \
  -d '{"site_id": "demo-site-001", "days": 7}'
```

---

## ❓ 常见问题

**Q: Docker 服务启动失败？**
```powershell
# 检查端口占用
netstat -ano | findstr "8000 8001 8002 5432"

# 清理并重启
docker-compose down -v
docker-compose up -d --build
```

**Q: Flutter 无法连接到后端？**
检查 `lib/core/api/config.dart` 中的 `apiBaseUrl`:
```dart
const apiBaseUrl = 'http://localhost:8000';  // 本地
// const apiBaseUrl = 'https://api.your-domain.com';  // 生产环境
```

**Q: 数据库初始化失败？**
```powershell
# 手动初始化
docker exec -it infra-db-1 psql -U postgres -d sedai -f /docker-entrypoint-initdb.d/init.sql
```

---

## 📞 联系方式

如需帮助或有问题，请联系项目团队。

**项目仓库**: (待添加 GitHub 链接)
**文档**: `docs/` 目录
**许可**: MIT License
